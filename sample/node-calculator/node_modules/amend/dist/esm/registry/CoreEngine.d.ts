export interface DecodedKey {
    key: string;
    errorClass: string;
    filePath: string;
    line: number;
    funcName: string;
    message: string;
}
/**
 * Build a self-decoding Amend key.
 *
 * Returns: `"amend:{CLASS}.{loc_b64u}.{msg_b64u}"`
 *
 * Properties:
 *   - Deterministic: same inputs → same key, always.
 *   - Reversible: decode() recovers all original fields.
 *   - Fast: 4 string ops, 2 Buffer calls, zero heap growth.
 */
export declare function encode(filePath: string, line: number, className: string, funcName: string, message: string, errorClass: string): string;
/**
 * Decode an Amend key back to its original components.
 *
 * @throws {Error} if key does not match the amend: format.
 */
export declare function decode(key: string): DecodedKey;
/**
 * Return true if key is a structurally valid amend: key.
 */
export declare function verify(key: string): boolean;
/** Fast extract of errorClass without full decode. */
export declare function errorClassFrom(key: string): string;
/** Fast extract of location fields only (filePath, line, funcName). */
export declare function locationFrom(key: string): Pick<DecodedKey, 'filePath' | 'line' | 'funcName'>;
/** Fast extract of message only. */
export declare function messageFrom(key: string): string;
export declare const CoreEngine: {
    encode: typeof encode;
    decode: typeof decode;
    verify: typeof verify;
    errorClassFrom: typeof errorClassFrom;
    locationFrom: typeof locationFrom;
    messageFrom: typeof messageFrom;
};
//# sourceMappingURL=CoreEngine.d.ts.map