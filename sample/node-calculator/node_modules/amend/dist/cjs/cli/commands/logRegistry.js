"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.logRegistryCommand = logRegistryCommand;
const Registry_js_1 = require("../../registry/Registry.js");
function logRegistryCommand(program) {
    program
        .command('logRegistry <filepath>')
        .description('Export all registered errors to a file (.json or .log)')
        .action((filepath) => {
        const cleanPath = filepath.replace(/^--/, '');
        let format;
        if (cleanPath.endsWith('.json')) {
            format = 'json';
        }
        else if (cleanPath.endsWith('.log')) {
            format = 'log';
        }
        else {
            console.log('Unsupported file extension. Use .json or .log');
            return;
        }
        Registry_js_1.Registry.getInstance().exportTo(cleanPath, format);
        console.log(`Registry exported to ${cleanPath} (${format} format)`);
    });
}
//# sourceMappingURL=logRegistry.js.map