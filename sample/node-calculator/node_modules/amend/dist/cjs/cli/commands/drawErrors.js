"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.drawErrorsCommand = drawErrorsCommand;
const Registry_js_1 = require("../../registry/Registry.js");
function drawErrorsCommand(program) {
    program
        .command('drawErrors')
        .description('List all registered errors (key + message)')
        .action(() => {
        const records = Registry_js_1.Registry.getInstance().getAll();
        if (records.length === 0) {
            console.log('No errors registered.');
            return;
        }
        const keyWidth = 55;
        console.log('KEY'.padEnd(keyWidth) + 'MESSAGE');
        for (const r of records) {
            const shortKey = r.key.length > keyWidth
                ? '…' + r.key.slice(-(keyWidth - 1))
                : r.key;
            console.log(shortKey.padEnd(keyWidth) + r.message);
        }
    });
}
//# sourceMappingURL=drawErrors.js.map